# MediaPro - 视频图片处理平台（前端）

## 📋 项目简介

专业的视频和图片处理平台前端项目，基于 Next.js 14 + React 18 + TypeScript + shadcn/ui 构建。

---

## ⚡ 快速开始

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm run dev

# 访问
http://localhost:3000/zh      # 中文
http://localhost:3000/en      # 英文
```

---

## 🛠️ 技术栈

- **框架**: Next.js 14 (App Router) + React 18
- **语言**: TypeScript 5
- **样式**: Tailwind CSS + shadcn/ui
- **状态管理**: Zustand
- **国际化**: next-intl
- **表单**: React Hook Form + Zod
- **网络请求**: Axios
- **图标**: lucide-react

---

## 📂 项目结构

```
src/
├── app/[locale]/          # 多语言路由
├── components/            # 组件
│   ├── common/           # 通用组件
│   ├── layout/           # 布局组件
│   ├── features/         # 功能组件
│   └── ui/               # shadcn/ui 组件
├── services/api/         # API 服务层
├── stores/               # 状态管理
├── types/                # 类型定义
├── constants/            # 常量
├── utils/                # 工具函数
├── hooks/                # 自定义 Hooks
└── i18n.ts              # 国际化配置
```

---

## 📚 开发规范

**重要**: 请查看项目规则文件了解完整的开发规范：

```
.cursor/rules/frontend-project-rules.md
```

包含：
- ✅ 完整的目录结构说明
- ✅ 代码规范和命名规则
- ✅ shadcn/ui 组件使用指南
- ✅ 国际化开发规范
- ✅ API 服务使用方式
- ✅ 状态管理指南
- ✅ 最佳实践和示例代码

---

## 🔧 可用命令

```bash
# 开发
pnpm run dev               # 启动开发服务器

# 构建
pnpm run build            # 生产构建
pnpm run start            # 启动生产服务器

# 代码质量
pnpm run lint             # ESLint 检查
pnpm run lint:fix         # 自动修复 ESLint 问题
pnpm run type-check       # TypeScript 类型检查
pnpm run format           # Prettier 格式化
pnpm run format:check     # 检查格式化

# shadcn/ui
npx shadcn@latest add [component-name]   # 添加组件
```

---

## 🌍 国际化

项目支持中英文双语：

- **中文**: `/zh/*`
- **英文**: `/en/*`

翻译文件位于 `messages/` 目录：
- `messages/zh.json` - 中文翻译
- `messages/en.json` - 英文翻译

---

## 🎨 UI 组件

使用 shadcn/ui 组件库，已安装15个组件：

- 表单: Button, Input, Label, Form, Select
- 展示: Card, Alert, Badge, Progress
- 交互: Dialog, Dropdown Menu, Toast
- 其他: Separator, Tabs

详细使用方法请查看 `SHADCN_USAGE.md`

---

## 📖 相关文档

- `.cursor/rules/frontend-project-rules.md` - **开发规范（必读）**
- `SHADCN_USAGE.md` - UI 组件使用指南
- `REFACTORING_SUMMARY.md` - 工程化重构总结

---

## 🚀 功能特性

- ✅ 视频/图片格式转换
- ✅ 文件压缩和调整大小
- ✅ 视频裁剪和合并
- ✅ 音频提取
- ✅ GIF 转换
- ✅ 处理历史记录
- ✅ 用户认证（本地 + Google OAuth）
- ✅ 文件拖放上传
- ✅ 中英文双语支持
- ✅ 深色模式支持（配置中）
- ✅ 响应式设计

---

## 🔐 环境变量

创建 `.env.local` 文件：

```env
NEXT_PUBLIC_API_URL=http://localhost:5000
```

---

## 📝 开发注意事项

1. **所有路由必须包含语言前缀** (`/zh/*`, `/en/*`)
2. **使用新的 services 和 stores 结构** (旧的 `lib/api.ts` 和 `lib/store.ts` 保留兼容)
3. **优先使用 shadcn/ui 组件**，而不是手写组件
4. **使用 TypeScript 类型定义**，避免 `any`
5. **所有文本必须使用翻译**，不要硬编码

---

## 🧪 测试

```bash
# 运行所有检查
pnpm run lint && pnpm run type-check && pnpm run build
```

---

## 📦 部署

```bash
# 构建生产版本
pnpm run build

# 启动生产服务器
pnpm run start
```

---

## 🤝 贡献

请遵循 `.cursor/rules/frontend-project-rules.md` 中的开发规范。

---

**版本**: 1.0.0  
**最后更新**: 2025-10-27  
**License**: MIT
